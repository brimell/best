declare module '*/routes/auth' {
    import { Router } from 'express';
    const router: Router;
    export default router;
}

declare module '*/routes/items' {
    import { Router } from 'express';
    const router: Router;
    export default router;
}

declare module '*/routes/categories' {
    import { Router } from 'express';
    const router: Router;
    export default router;
}

declare module '*/routes/marketplace' {
    import { Router } from 'express';
    const router: Router;
    export default router;
}

declare module '*/routes/transactions' {
    import { Router } from 'express';
    const router: Router;
    export default router;
}

declare module '*/routes/ratings' {
    import { Router } from 'express';
    const router: Router;
    export default router;
} 
declare module '*/routes/templates' {
    import { Router } from 'express';
    const router: Router;
    export default router;
}

declare module '*/routes/lists' {
    import { Router } from 'express';
    const router: Router;
    export default router;
}
